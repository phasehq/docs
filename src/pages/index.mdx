import { FramewroksIntegration } from '@/components/FramewroksIntegration'
import { PlatformsIntegration } from '@/components/PlatformsIntegration'

import { Deployments } from '@/components/Deployments'
import { Guides } from '@/components/Guides'
import { SecurityResources } from '@/components/SecurityResources'
import { HeroPattern } from '@/components/HeroPattern'
import AsciinemaEmbed from '@/components/AsciinemaEmbed'
import { Tag } from '@/components/Tag'

export const description =
  'Phase is an open source (open core) end-to-end encrypted platform for creating, managing, syncing and deploying secrets and environment variables across your infrastructure.'

<HeroPattern />

<Tag variant="small">GETTING STARTED</Tag>

# Introduction

Phase is an open source (open core) end-to-end encrypted platform for creating, managing, deploying secrets and environment variables across your infrastructure. {{ className: 'lead' }}

## Phase CLI

Import, encrypt and inject secrets in your application within seconds. Zero code change required.

<div
  dangerouslySetInnerHTML={{
    __html: `
    <script async id="asciicast-607829" src="https://asciinema.org/a/607829.js"></script>
  `,
  }}
></div>

Get the CLI

<CodeGroup>

```fish {{ title: 'Pip' }}
pip install phase-cli
```

```zsh {{ title: 'MacOS' }}
brew install phasehq/cli/phase
```

```fish {{ title: 'Windows' }}
scoop bucket add phasehq https://github.com/phasehq/scoop-cli.git
scoop install phase
```

```fish {{ title: 'Alpine Linux' }}
curl -fsSL https://pkg.phase.dev/install.sh | bash
```

```fish {{ title: 'RedHat/CentOS/Amazon Linux' }}
curl -fsSL https://pkg.phase.dev/install.sh | bash
```

```fish {{ title: 'Ubuntu/Debian' }}
curl -fsSL https://pkg.phase.dev/install.sh | bash
```

```fish {{ title: 'Arch Linux' }}
curl -fsSL https://pkg.phase.dev/install.sh | bash
```

```fish {{ title: 'Docker' }}
# Run the latest version
docker run phasehq/cli

# Optional: run a specific version - docker run phasehq/cli:<version>
```

</CodeGroup>

<div className="not-prose">
  <Button
    href="/cli/install"
    variant="text"
    arrow="right"
    children="Explore CLI Installation"
  />
</div>

## Integrate Phase

Seamlessly work with your favorite framework.

<FramewroksIntegration />

<div className="not-prose">
  <Button
    href="/integrations"
    variant="text"
    arrow="right"
    children="Explore Framework Integrations"
  />
</div>

Seamlessly work with your preferred platform.

<PlatformsIntegration />

<div className="not-prose">
  <Button
    href="integrations/platforms/docker"
    variant="text"
    arrow="right"
    children="Explore Platforms Integrations"
  />
</div>

## Self-hosting options

Deploy Phase on your own infrastructure, maintain full control over your data.

<Deployments />

<div className="not-prose">
  <Button
    href="/self-hosting"
    variant="text"
    arrow="right"
    children="Explore Deployment Options"
  />
</div>
---

## Explore Phase security

Phase is not a black box. Learn more about security and architecture:

<SecurityResources />

---

## Ready to try?{{ anchor: true }}

To get started, create a new application in your [Phase Console](https://console.phase.dev) and set up the [Phase CLI](/cli/usage). {{ className: 'lead' }}

<div className="not-prose mb-16 mt-6 flex gap-3">
  <Button href="/quickstart" arrow="right" children="🚀 Quickstart" />
  <Button
    href="/integrations"
    variant="outline"
    children="Explore Integrations"
  />
</div>
